node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
mcp-todos.json
test-results
playwright-report
.playwright

# IBILS auth fixture — secret sidecars + run-scoped runtime meta + storageState (never commit)
.artifact/e2e-auth-secrets.json
.artifact/e2e-auth-secrets-*.json
.artifact/e2e-auth-runtime.json
.artifact/e2e-auth-runtime-*.json
.artifact/e2e-auth-storage-*.json
# Legacy shared Playwright storageState (CLI flows; Playwright uses run-scoped path above)
qa/e2e/fixtures/storage/admin.json

# board data: publish ONLY the demo board + committed index + conventions.
# your private boards (boards.local.json + their dirs) stay local.
data/*
!data/boards
!data/boards.json
!data/conventions.json
data/boards/*
!data/boards/demo
data/boards.local.json
