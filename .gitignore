node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
mcp-todos.json
test-results
playwright-report
.playwright

# board data: publish ONLY the demo board + committed index + conventions.
# your private boards (boards.local.json + their dirs) stay local.
data/*
!data/boards
!data/boards.json
!data/conventions.json
data/boards/*
!data/boards/demo
data/boards.local.json
