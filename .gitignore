node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
mcp-todos.json
test-results
playwright-report
.playwright

# IBILS auth fixture — secret sidecars + run-scoped runtime meta (never commit)
.artifact/e2e-auth-secrets.json
.artifact/e2e-auth-secrets-*.json
.artifact/e2e-auth-runtime.json
.artifact/e2e-auth-runtime-*.json
# Canonical Playwright storageState produced by setup-auth (also ignored under storage/.gitignore)
qa/e2e/fixtures/storage/admin.json

# board data: publish ONLY the demo board + committed index + conventions.
# your private boards (boards.local.json + their dirs) stay local.
data/*
!data/boards
!data/boards.json
!data/conventions.json
data/boards/*
!data/boards/demo
data/boards.local.json
